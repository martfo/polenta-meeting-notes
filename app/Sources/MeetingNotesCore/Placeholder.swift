// MeetingNotesCore holds the testable, UI-free logic of the app: the tap
// description spec, the audio mixer, level metering, the in-person classifier,
// recording coordination, first-run provisioning, and vault creation. Filled
// in slice by slice.

public enum MeetingNotesCore {
    public static let name = "MeetingNotesCore"
}
